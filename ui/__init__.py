"""HAPPY AI Agent — UI package.

Built fresh against the new-desktop-project playbook (ENA Conquest
Desktop v2.0.0 -> v2.6.3 patterns): core/ + ui/ + tools/ split,
geometry persistence, single-instance lock, system tray, Pillow color
emoji, solid status dots, render-hash skip, cards-first layout.

Dark theme, English labels. Backend (auth, pipeline, agents, builder,
updater, extractor, file_loader) lives at the project root and is
imported from here unchanged.
"""

# Credentials — NEVER commit
*.json
!package.json
!.streamlit/*.json
service-account*.json
credentials*.json
*.key
*.pem
.env
.env.*
admin_pat.json
auth.json

# Sessions (user data)
sessions/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Logs
*.log

# PyInstaller build artifacts (large — 316 MB + 73 MB)
dist/
build/

# Installer build outputs — .iss script + assets ใน installer/ ส่งขึ้น git ได้
# แต่ compiled .exe ใหญ่เกินไป ไม่ commit
installer/output/

# Release archive — .exe/.zip ใหญ่ ไม่ commit (แต่ release-notes.md เก็บได้)
releases/*/*.exe
releases/*/*.zip

# Claude Code worktree — internal use, อย่า commit
.claude/worktrees/

# Personal files ที่หลุดเข้าโปรเจกต์ — gitignored explicit (ไม่ใช่ HAPPY source)
Nick_Creative_Portfolio.docx
ระบบดาวเทียม.html

# Backups
*.bak
*.bak[0-9]*

# Test screenshots — regeneratable by running ui_verify.py / bug5_*.py
ui_screenshots/

# Local auth (defense in depth — real file lives in ~/.happy/)
.happy/

# Archive folder — contains old GCP service account + personal data
archive/

# QA / scratch artefacts that shouldn't ship
happy_pid.tmp
*.spec.bak
ui_verify.py
ui_verify_results.json
qa_final.py
test_pipeline_e2e.py
lifecycle_test.py
WORKIE_NOTES.md
WEB_TEST_RESULTS.md
HAPPY_AI_AGENT_HANDOFF.md
HANDOFF.md
HANDOFF_ARCHIVE_coddy1to4.md
ONBOARD_NEW_CODDY.md
ONBOARD_NEW_COSS.md
app.py
app.py.bak3
happy_desktop.py
streamlit_stderr.log
streamlit_stdout.log
installer-stderr.log
installer-stdout.log
se.log
so.log
installer-mockup.html

# Old/unused inside installer/ (kept on disk, not in repo)
installer/HAPPY.iss
installer/license-en.txt
installer/license-th.txt
installer/wizard-image.bmp
installer/wizard-small.bmp
installer/happy.ico
